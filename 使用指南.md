# Scrcpy Android 使用指南

控制端装本 App，被控端开无线 ADB。两机同一 Wi‑Fi。

## 当前已打通的设备（示例）

| 角色 | 机型 | 地址 |
|------|------|------|
| **控制端** | 24031PN0DC（小米） | `192.168.0.161:5555` |
| **被控端** | 2312DRA50C（红米） | `192.168.0.55:5555` |

在控制端 App 里填写：

- IP：`192.168.0.55`
- 端口：`5555`
- 分辨率：建议「适配本机屏幕」

然后点「连接并投屏」。被控端若弹出「允许 USB 调试」，点允许（可勾选始终允许）。

---

## 以后自己操作：被控端首次开启无线 ADB

被控机关机/换网后，无线 ADB 可能失效，需要再开一次。

### 方法一：USB + 电脑（最稳）

1. 被控机开启「开发者选项 → USB 调试」
2. USB 连电脑，确认电脑能看到设备：

```bash
adb devices
```

3. 打开 TCP 5555：

```bash
adb -s <序列号或IP:端口> tcpip 5555
```

4. 查被控机 Wi‑Fi IP（设置 → WLAN，或）：

```bash
adb -s <序列号> shell ip -f inet addr show wlan0
```

记下类似 `192.168.0.xx` 的地址。

5. 电脑验证无线连接：

```bash
adb connect 192.168.0.xx:5555
adb devices
```

6. 可拔掉 USB。之后控制端 App 填这个 IP 即可。

### 方法二：被控机系统自带「无线调试」（Android 11+）

1. 开发者选项 → **无线调试** → 开启  
2. 记下其中的 **IP 地址与端口**（连接用端口，不是配对端口）  
3. 若控制端是首次连这台机，需先在电脑或支持配对的工具上完成配对；本 App 当前走的是已授权的 ADB 连接（`tcpip 5555` 方式最省事）

**推荐继续用方法一（tcpip 5555）。**

---

## 安装 / 更新控制端 App

电脑已连上控制端时：

```bash
adb -s 192.168.0.161:5555 install -r app\build\outputs\apk\debug\app-debug.apk
```

若控制端是 USB：

```bash
adb -s <控制端序列号> install -r app\build\outputs\apk\debug\app-debug.apk
```

重新编译 APK：

```powershell
set JAVA_HOME=C:\Program Files\Android\Android Studio\jbr
.\gradlew.bat assembleDebug
```

产物路径：

`app\build\outputs\apk\debug\app-debug.apk`

---

## 控制端投屏步骤（日常）

1. 确认被控端无线 ADB 仍可用（同一 Wi‑Fi，IP 可能变化）  
2. 打开控制端 **Scrcpy Android**  
3. 填被控 IP / 端口  
4. 选分辨率（默认「适配本机屏幕」）  
5. 连接；被控端授权（如有）  
6. 触控画面；系统返回键会注入到被控端；划掉任务即可退出本 App  

### IP 变了怎么办？

被控机重连 Wi‑Fi 后 IP 可能变。USB 连电脑再查：

```bash
adb shell ip -f inet addr show wlan0
```

或到手机「设置 → WLAN → 当前网络」查看。

---

## 常用排错

| 现象 | 处理 |
|------|------|
| 连接失败 / timeout | 同网段？被控是否还在 `5555`？防火墙/AP 隔离？ |
| 提示未授权 | 被控端点「允许 USB 调试」 |
| `adb connect` 拒绝 | 再执行一次 `adb tcpip 5555`（需 USB） |
| 有画面无触控 | 看控制通道是否连上；试断开重连 |
| 画面模糊/卡 | 分辨率改 720/1080；两机尽量同 AP |

验证被控端（在电脑上）：

```bash
adb connect 192.168.0.55:5555
adb -s 192.168.0.55:5555 shell echo ok
```

---

## 角色对照（记住即可）

- **161（控制端）**：装 App，看对方屏幕、点对方屏幕  
- **另一台（被控端）**：只开无线 ADB，**不用装**本 App；由控制端推送 `scrcpy-server`
